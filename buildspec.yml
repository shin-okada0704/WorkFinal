version: 0.2

phases:
  build:
    commands:
      - aws deploy push --application-name work-kadai --s3-location s3://workfinal0608/artifact.zip --source .
artifacts:
  files:
    - '**/*'
  base-directory: src
